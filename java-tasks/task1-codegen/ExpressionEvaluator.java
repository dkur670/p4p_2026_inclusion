public class ExpressionEvaluator {
    /**
     * Evaluates a mathematical expression given as a string and returns the result.
     *
     * Supported: integers and decimals, the operators + - * /, unary minus,
     * parentheses, and standard operator precedence (* and / bind tighter
     * than + and -; operators of the same precedence evaluate left to right).
     * Whitespace anywhere in the expression should be ignored.
     *
     * Throws ArithmeticException if the expression divides by zero.
     * Throws IllegalArgumentException if the expression is not a valid
     * expression (e.g. empty input, mismatched parentheses, invalid
     * characters, or a missing operand).
     *
     * Examples:
     *   "2 + 3 * 4"         -> 14.0
     *   "(2 + 3) * 4"       -> 20.0
     *   "10 / 2 - 3"        -> 2.0
     *   "-5 + 3"            -> -2.0
     *   "2 * (3 + (4 - 1))" -> 12.0
     *
     * @param expression the expression to evaluate
     * @return the numeric result of the expression
     */
    public double evaluate(String expression) {
        // Implement your solution here
        return 0;
    }
}
